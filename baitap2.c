# include <stdio.h>
int main(){
   int so_cho_truoc=21,so_nhap_vao;
    if(so_nhap_vao>0){
	
	do{
    printf("nhap mot so: ");
   	scanf("%d",&so_nhap_vao);
	}while(so_cho_truoc != so_nhap_vao);
	  	printf("so dc nhap trung voi so cho truoc");
	  	
	}   
	 else{
	 	printf("loi");
	 }
	

return 0;
	}
